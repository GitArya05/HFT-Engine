# docker/ (reserved — Stage 10)

`docker-compose.yml` + Dockerfiles bundling the gateway and dashboard for a
one-command demo of the full stack. This is separate from, and does not
replace, the "no containers, honest VM" rule for the engine's real Stage 7
benchmark runs — those still happen directly on the EC2 instance.

Reserved and empty until Stage 10.
