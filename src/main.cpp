#include <cstdio>

// Stage 0 placeholder. Proves the compiler, CMake config, and presets work.
// Replaced with the real server entry point (IOCP event loop, engine
// startup) starting in Stage 5.
int main() {
    std::puts("HFT matching engine - Stage 0 skeleton build OK");
    return 0;
}
